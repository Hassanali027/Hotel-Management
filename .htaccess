<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^(images|uploads|assets)(/.*)?$ public/$1$2 [L]
    RewriteRule ^(favicon\.ico|robots\.txt)$ public/$1 [L]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
